{
  "name": "copilot-dash-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Copilot Dash Edge Extension",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.304",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
